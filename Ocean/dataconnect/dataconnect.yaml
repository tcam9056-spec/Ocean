specVersion: "v1"
serviceId: "workspace"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "workspace-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
